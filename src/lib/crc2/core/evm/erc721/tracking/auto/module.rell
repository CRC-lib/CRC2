module;

import ^^.tracking;
import ^^^^.evm;

@extend(tracking.handle_untracked_token)
function (network_id: integer, event: tracking.transfer_event) {
    val evm_token = evm.ensure_evm_token(network_id, event.contract_address, event.token_id);
    tracking.set_owner(evm_token, event.to);
}
